<%= bootstrap_form_for @project do |f| %>
  <%= f.text_field :name %>

  <%= f.text_area :description %>

  <%= f.text_field :url, prepend: "http://www." %>

  <%= f.primary yield(:submit_text) %>
<% end %>
