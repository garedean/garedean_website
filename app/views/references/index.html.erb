<header class="page-header">
  <h1 class="page-heading text-center">
    Friends
  </h1>
</header>

<p class="text-justify">The following is a collection of <span class="line-through">shameless plugs</span> references from people I've worked with in the past. Most of these are from '360 performance review' responses anonymously collected from previous coworkers.</p>

<hr>

<% if admin? %>
  <%= bootstrap_form_for @reference do |f| %>
    <%= f.text_area :quote, autofocus: true %>
    <%= f.submit %>
  <% end %>
<% end %>

<ul class="references-list">
  <% for reference in @references %>
    <li id="reference_<%= reference.id %>">
      <%= reference.quote %> <%= link_to "(delete)", reference_path(reference), class: "text-danger", method: :delete %>
    </li>
  <% end %>
</ul>
