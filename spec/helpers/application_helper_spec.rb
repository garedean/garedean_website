require 'rails_helper'

# I don't know how to add users given the complexity Devise introduces, so for now, I'm skipping writing these tests
describe ApplicationHelper do
  it "returns true if a current user is admin" do

  end

  it "returns false if a current user is not admin" do

  end
end
